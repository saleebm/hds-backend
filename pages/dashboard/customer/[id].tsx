import { DashboardView } from '@Components/Views/dashboard'

export default function () {
  return (
    <DashboardView pageTitle={`Customer Report`}>
      <p>
        TODO: customer report - that includes all general information about the
        customer and what products they have purchased. This is not the
        Statement.
      </p>
    </DashboardView>
  )
}
